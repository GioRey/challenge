import 'package:flutter/material.dart';

class NoCounters extends StatelessWidget {
  const NoCounters({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('No tienes contadores'));
  }
}
